# =============================
# FRONTEND (Vite)
# =============================
# Em produção, deixe assim para usar a mesma origem do site:
# VITE_API_BASE_URL=/api
# VITE_USE_MOCK=false

# =============================
# BACKEND (Node/Express)
# =============================
PORT=3000
JWT_SECRET=troque-por-um-segredo-bem-grande
JWT_EXPIRES_IN=7d

# MySQL (Hostinger)
DB_HOST=
DB_PORT=3306
DB_USER=
DB_PASSWORD=
DB_NAME=

# Uploads
UPLOAD_DIR=uploads
# 25MB default
UPLOAD_MAX_BYTES=26214400

# CORS (opcional). Se vazio, permite tudo.
# CORS_ORIGIN=https://seu-dominio.com

# Admin inicial (opcional). Se não definir, use POST /api/auth/bootstrap uma vez.
# ADMIN_USERNAME=admin
# ADMIN_PASSWORD=senha-forte
